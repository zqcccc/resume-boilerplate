# 2026-09-09

## 简历去除「22 万行代码」量化表述
- 用户要求：不再提「22 万行代码」，最多提「主要开发者」。
- 已清理：自我介绍_AI-Agent岗.md、自我介绍_全栈后端岗.md、src/index.html、build/index.html（共 12 处，中英双语）。
- PDF 也含旧文本（public/ + build/ 各中英文 2 个），已用 chromium headless 重新生成覆盖。

## PDF 再生命令（关键）
- 源：`src/index.html`（默认 `lang-zh`；英文需临时把 `<html ... class="lang-zh">` 改为 `lang-en`）。
- 工具：`/opt/homebrew/bin/chromium`（v115，裸机直接跑会崩）。
- 可用参数：
  `chromium --headless --no-sandbox --disable-gpu --disable-dev-shm-usage --no-zygote --no-pdf-header-footer --user-data-dir=/tmp/cp_xx --print-to-pdf=OUT.pdf file:///abs/src/index.html`
  - 必须用 `--headless`（旧版，非 --headless=new）+ `--no-zygote`，否则 mach rendezvous 崩溃/exit 21。
  - 先 `rm -f ~/Library/Application\ Support/Chromium/SingletonLock` 清 stale 锁。
- 中文 PDF 2 页、英文 4 页；生成后需 cp 到 public/ 与 build/ 两处保持一致。
- 校验：pypdf 抽取文本 grep `220,000|22\s*万行|代码变更量`。pypdf 已装到托管 python `/Users/gongzhao/.workbuddy/binaries/python/versions/3.13.12/bin/python3`。
