# 2026-09-13

## 简历重构：结果导向内容 + 简洁排版（commit 086426d）

### 内容（全部基于 ~/code 真实代码与 git 记录核实，无虚构）

| 时期 | 仓库 | 提交数 | 简历落点 |
|---|---|---|---|
| 映客 | `~/code/ccode/boc.inke.cn.react1`（无 git） | 名下 24 个 entry（`src/views/config-pro/innerapp-2020/2021.js` 里 `@author 赵翘楚`） | "24 个业务 entry" |
| 映客 | `ccode/IKRNBundle` | 31 | RN 任务中心 + 热更 |
| 映客 | `ccode/pay.busi.inke.com` | 16 | 支付前端 + ARMS 告警过滤 |
| 小冰 | `chararu-v2-app` | 454 | "核心仓库 450+ commits" |
| 小冰 | `IslandUX` / `_mp` / `IslandUX2` | 308/221/298 | 多平台小程序矩阵 |
| 小冰 | `kararu-op` + `karau-operation-api` | 44 + 15 | 运营后台全栈（前端+Node API） |
| 小冰 | `H5_XEva` | 16 | Apple 登录 / IAP / 阿拉伯语 |
| 蓝耘 | `lanyun-agent`（Bisheng 二开） | 127 | 数据广场 0→1、租户管理、MCP 接入 |
| 蓝耘 | `ai-chatbot`（蓝耘星河） | **611** | "团队提交量第一" |
| 滑雪 | `hxq`（monorepo） | 455 | Java→Go 重写并生产切流 |

- **小冰离职证明**精确日期：`2022-01-04 入职 ~ 2025-03-25 离职`，简历 `2022.01 ‒ 2025.03` 准确。
- **蓝耘 2026-06 离职**（裁员，简历不写原因）。空窗期由滑雪项目（`2026.05 ‒ 至今`）自然覆盖。
- **滑雪去已上线**（拼车/拼房/二手雪票），只是未推广、用户量少。简历写"已上线（尚未推广）"。
- 映客代码在 `~/code/ccode`（`code.inke.cn` 内网仓库），不在 `~/code` 顶层，也不在 Downloads。

### 排版重设计要点
- 去卡片背景/圆角/hover 阴影 → 留白 + 细分割线；颜色收敛为 4 级灰阶。
- 标题改小型大写 + `letter-spacing`；正文行高 1.68。
- 语言切换按钮改半透明玻璃质感。

### 打印 CSS 技术债修复（重要，覆盖 2026-08-22 的旧记录）
- **旧问题**：`.page` 固定 `min-height: 297mm` + `.page-break{page-break-before:always}`，
  导致生成 PDF 时中段出现空白页，每次都要手工复制一份改 `min-height:0` 打补丁。
- **已修复**：新样式里 `.page` 用 `min-height: 0` 自然流布局 + `break-inside: avoid`
  （作用于 `.exp-group/.summary-item/.skill-row/.proj-header/.edu-header/.exp-header`）。
- **现在直接从 `src/index.html` 生成即可，无需任何补丁**。中英 PDF 均 3 页、无空白页。

### PDF 生成命令（已验证）
```bash
rm -f ~/Library/Application\ Support/Chromium/SingletonLock
/opt/homebrew/bin/chromium --headless --no-sandbox --disable-gpu --disable-dev-shm-usage \
  --no-zygote --no-pdf-header-footer --user-data-dir=/tmp/CPX \
  --print-to-pdf=OUT.pdf "file:///abs/src/index.html"
# 英文版：sed 's|class="lang-zh"|class="lang-en"|' src/index.html > /tmp/index_en.html 再渲染
# 必须 --headless（旧版）+ --no-zygote，否则 mach rendezvous 崩溃
```
- pdftotext 在：`/Users/gongzhao/Library/Application Support/TRAE SOLO CN/ModularData/ai-agent/vm/tools/opt/poppler/26.06.0/bin`

### 其他
- 标题定位由「前端开发工程师」改为「前端 / AI 应用工程师」（目标 AI Agent 岗）。
  三家公司内的历史职位名保持"前端开发工程师"原样，未篡改。
- `.gitignore` 增加 `.pi/` 与 `.workbuddy/`（本地会话数据，不入库）。`build/` 仍被忽略，由 CI 生成。
- 部署：push `master` → Actions 构建 → 自定义域 **https://resume.onlylike.work/**
- 裁员话术已写入两份 `自我介绍_*.md`（Q5 空窗期 / Q6 离职原因），核心是"以项目产出回应，绝不主动说被裁"。
