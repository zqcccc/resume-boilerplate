# 2026-08-22

## 简历项目：中英双语 + 英文 PDF

- `src/index.html` 改造为双语：每个文本片段包成 `<span class="zh">` / `<span class="en">`，用 `html.lang-zh` / `html.lang-en` 经 CSS 切换隐藏非激活语言（默认 `lang-zh`）。
- 新增屏幕专属语言切换按钮 `#langToggle`（固定右上角，`@media print` 隐藏），含 `localStorage` 记忆与 `<title>` 同步。
- 字体栈前置 `Helvetica Neue/Arial` 以优化英文排版，中文回退到 PingFang/Noto。
- 英文 PDF：`public/Zhao_Qiaochu_Resume_EN.pdf`（A4，4 页）。生成方式：复制 index.html 并将 `class` 改 `lang-en`，用 `/opt/homebrew/bin/chromium --headless=old --no-pdf-header-footer --print-to-pdf` 渲染（注意：new-headless 会挂起，且需 `--user-data-dir` 避免 SingletonLock 冲突）。
- 验证：英文 PDF 0 个中文字符；中文渲染 2 页 1525 中文字符，无回归。
